# sample-data

ダミーデータ・検証用素材のためのディレクトリです。

- **実際の360°動画・現場データを置かないでください**(Git管理対象のため)
- 案件・動画のメタデータは現在、データディレクトリ
  (既定: `<ホーム>/LocalSiteWalkData`)内のSQLite `local_site_walk.db` に保存されます
- 動作確認は、アプリの「新規案件を登録」でローカルフォルダを指定し、
  「フォルダをスキャン」を実行してください
