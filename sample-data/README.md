# sample-data

ダミーのメタデータのみを置くディレクトリです。

- **実際の360°動画・現場データを置かないでください**(Git管理対象のため)
- `projects.sample.json` は案件メタデータの書式例です。
  動作確認する場合は、データディレクトリ(既定: `<ホーム>/LocalSiteWalkData`)へ
  `projects.json` という名前でコピーしてください
