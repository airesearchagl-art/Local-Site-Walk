-r requirements.txt
pytest>=8.0
# starlette.testclient (used via fastapi.testclient in tests) prefers
# httpx2 and only falls back to httpx with a StarletteDeprecationWarning
# ("Using `httpx` with `starlette.testclient` is deprecated; install
# `httpx2` instead."). httpx2 is the package Starlette itself recommends
# installing, so this is a drop-in swap of the test-only HTTP client
# dependency, not an application code change.
httpx2>=2.0,<3.0
ruff>=0.8
